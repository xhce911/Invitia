import React, { Component } from 'react'


export default class Photo extends Component {

  render() {
    return (
        <section id={'parallax-'+this.props.number} className="hero is-fullheight">
       
        </section>
    )
  }
}
